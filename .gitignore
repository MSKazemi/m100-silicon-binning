__pycache__/
*.pyc
.venv/
venv/

# extracted dataset partitions — regenerate from the Zenodo archives
ipmi/
ipmi2103/
*.tar

# LaTeX build artefacts
*.aux
*.log
*.out
*.bbl
*.blg

# in-flight sweep extraction
.sweep_tmp/
sweep.log

# large extraction scratch dirs (never commit)
.therm/
.a2/
.a1_tmp/

# power/OS sweep scratch and derived tables
.pw_tmp/
cov_all/
.cov_tmp/

# private working material — internal peer reviews, the issue register and the
# state-of-the-art dossier stay local and are deliberately not published
reviews/
design/
OPEN_ISSUES.md
paper/review_*.md
